/**
 * @internal
 */
export enum InternalCommandOption {
    SUB_COMMAND	= 1,
    SUB_COMMAND_GROUP = 2,
}

export enum CommandOption {
    STRING = 3,
    INTEGER = 4,
    BOOLEAN = 5,
    USER = 6,
    CHANNEL = 7,
    ROLE = 8,
    MENTIONABLE = 9,
    NUMBER = 10
}
